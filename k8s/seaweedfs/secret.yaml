apiVersion: v1
kind: Secret
metadata:
  name: seaweedfs-s3-secret
  namespace: mlops
type: Opaque
stringData:
  # Base64-encode your actual credentials before applying
  accessKey: "minioadmin"
  secretKey: "minioadmin"
